{% extends 'base.html.twig' %}

{% block title %}Edit AdminUser{% endblock %}

{% block body %}
    <h1>Edit AdminUser</h1>

    {{ include('admin_user/_form.html.twig', {'button_label': 'Update'}) }}

    {{ include('admin_user/_delete_form.html.twig') }}

    <div class="mt-4">
        <a href="{{ path('app_admin_user_index') }}" class="inline-block bg-gray-200 text-blue-500 font-bold py-2 px-4 rounded focus:outline-none focus:shadow-outline hover:bg-gray-300 hover:text-blue-700 hover:underline">
            Back to List
        </a>
    </div>
{% endblock %}
