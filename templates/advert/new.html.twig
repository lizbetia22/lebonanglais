{% extends 'base.html.twig' %}

{% block title %}New Advert{% endblock %}

{% block body %}
    <h1 class="text-3xl font-bold mb-6">Create new Advert</h1>

    <div class="bg-white rounded shadow-md p-6">
        {{ include('advert/_form.html.twig') }}
    </div>
    <a href="{{ path('app_advert_index') }}" class="block mt-4 text-blue-500 hover:text-blue-600">Back to list of adverts</a>
    <a href="{{ path('app_home') }}" class="block mt-4 text-blue-500 hover:text-blue-600">Home</a>
{% endblock %}
